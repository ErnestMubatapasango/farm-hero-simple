export default function DocumentsStep({ data, onChange }: { data: any; onChange: (d: any) => void }) {
  return (
    <div className="p-4 text-center text-muted-foreground">
      <p>Document upload will be available in Phase 2.</p>
    </div>
  );
}
